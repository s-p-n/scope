return 8 + 120 * 2 / !(5); // @expect 10.
/**
 * Expect:
 * The expression should evaluate to the number 10.
 * The program does not print the number ten, but instead returns it.
 * When another Scope program uses this one, the value returned to the
 * variable in the 'use/as' statement is expected to be 10.
 **/
