function $$$self%id% (access, name, value) {
        return void 0 === value ?
                ($$$parent%id%[access] || %error%) : (
                        $$$parent%id%.access[name] = access,
                        $$$parent%id%[name]=value,
                        value
)};
var $$$parent%id% = {
        access: {
                parent: "private"
        },
        parent: %value%
};