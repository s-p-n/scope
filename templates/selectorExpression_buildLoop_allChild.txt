	function loop ($$) {
		var i;
		for (i in $$) {
			if($$.hasOwnProperty(i) && %condition%) {
				%buildList%
			} else if(typeof $$[i] === "object"){
				loop($$[i])
	}}}.bind(this);
	loop($$);
	%returnRet%;
}.bind(this)(%context%))