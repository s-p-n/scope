(function () {
	this.$self("var", "%name%", "");
	this.$self("var", "%val%", null);
	var name, $$$list = %list%.$values["Array"]();
	for (%name% in $$$list) {
		this.$self("%name%", $primitive((typeof %name% === "String" ? "Text" : "Number"), function () {
			return %name%;
		}));
		if ($$$list.hasOwnProperty(%name%)) {
			this.$self("%val%", $$$list[%name%]);
			(function () {